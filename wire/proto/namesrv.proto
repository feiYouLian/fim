syntax = "proto3";
package wire;

message LgServer {
    string id = 1;
    string ip = 2;
    int32 port = 3;
}

message Gateway {
    string id = 1;
    string ip = 2;
    int32 port = 3;
    int32 protocol = 4; // 1.websocket 2.tcp
    bool ssl = 5;
}